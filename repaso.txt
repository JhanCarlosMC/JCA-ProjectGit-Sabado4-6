Conceptos de JavaScript
1. Tipos de variables
    var
    let - Variable - Cambia en tiempo de ejecucion
    const - Constante - Fijo desde el momento de su definicion
    // --> let cedula; Declaracion
    // --> cedula = "123"; Asignacion
    // --> let cedula = "123"; Inicializacion

2. Intregra HTML y CSS (Modificaciones desde js) manipulacion del DOM
    -document.getELementoById()
    -document.getElementQuerySelector()
    -document.createElement()
    --element.style.color = ""
    --element.classlist.add()
    --element.classlist.remove()

3. Tipos de datos
    Texto - String
    Numeros - Number
    Logicos - Boolean

4. Captura de eventos 
    - addEventListener("click")

5. Tipos de operadores
    Logicos - &&(y) ||(o) !(not)
    Numericos/Aritmeticos - + - * / ++ -- ** %
    Comparacion/Relacionales < > <= >= == ===
        2 == "2" -> True
        2 === "2" -> False

6. Estructuras de control
    if - if else - else if
        if(condicion){
            //Codigo en caso de verdadero
        }else{
            //Codigo en caso de falso
        }
    switch
7. Clase - Abstracto - Molde para creacion de objetos
8. Objeto - Concreto - Instancia de una clase
9. Estructuras ciclas/repeticion/bucles
    for -> for(let i = 0; i < 10; i++){}
    while -> while(condicion){ //Codigo }
    do-while -> do{ //Codigo }while(condicion)

10. Arreglos
    let arreglo = [1,2,3,4,5]; 
        index = 0...n-1

    console.log(arreglo[2])
    arreglo[0] = 0;